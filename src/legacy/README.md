# Legacy Cube App

This folder contains a preserved snapshot of the original cube-based portfolio UI.

It is kept "in reserve" for future reuse (cube controls + language switcher), but it is **not** used by the current runtime app.
